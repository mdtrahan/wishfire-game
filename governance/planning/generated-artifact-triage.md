# Generated Artifact Triage

Status: review packet for `ORKA-yib8.3`. This document classifies tracked ignored/generated artifacts and local caches. It does not authorize deletion, untracking, package changes, or backup cleanup.

## Current Evidence

Checked on 2026-06-28 from the main checkout before opening the `ORKA-yib8.3` worktree.

- `git ls-files -ci --exclude-standard` reports tracked ignored files under `node_modules/.package-lock.json` and `test-results/`.
- `git status --ignored --short` reports ignored local caches including `.fallow/`, `.serena/`, `.worktrees/`, `.netlify/`, `node_modules/`, `rust/simulation_core/target/`, `test-results/ORKA-oyi/`, and `web-runner/runtime-fingerprint.js`.
- `du -sh` reports approximate sizes: `node_modules/.package-lock.json` 4K, `output/web-game` 84K, `test-results` 760K, `.beads/backup` 21M, `package.json` 4K.
- `package.json` has `"main": "index.js"` and `index.js` is absent.
- `.beads/backup/` contains tracked `.darc` backup files plus manifest/lock/state files. Treat these as Beads recovery material, not generic generated cleanup.

## Candidate Matrix

| Candidate | Disposition | Evidence | Next safe action |
| --- | --- | --- | --- |
| `node_modules/.package-lock.json` | Untrack candidate | Tracked file matches ignored `node_modules/`; root `package-lock.json` is present and package dependency cleanup is not indicated by Fallow. | Create a narrow package-metadata cleanup bead. Validate `npm install`/lock behavior before untracking. No dependency upgrade/removal. |
| `output/web-game/shot-0.png` and `output/web-game/state-0.json` | Archive/untrack candidate | Generated proof artifacts under ignored output-style path; PNG is 396x704 and JSON is state output. | Preserve in a dated archive or documented artifact manifest before untracking/deleting. Do not remove without owner approval. |
| Tracked `test-results/ORKA-*` JSON/PNG proofs | Keep until proof-retention policy exists | These are bead-scoped validation artifacts and regression evidence even though `test-results/` is ignored. | Create a proof-retention policy bead to decide keep, archive, or untrack per Bead. Do not sweep all test results. |
| `test-results/.DS_Store` | Delete candidate | Apple Desktop Services file in ignored test-results tree, not validation proof. | Safe only in a tiny generated-trash cleanup bead with no runtime changes. |
| `.beads/backup/*.darc`, manifest, lock, state | Keep / blocked | 21M tracked recovery material; Bead notes already warn not to delete backups without explicit Beads backup policy. | Route to `ORKA-yib8.11` or a dedicated Beads backup-policy bead. Do not delete under generated-artifact cleanup. |
| `web-runner/runtime-fingerprint.js` | Keep ignored and untracked | Generated by `tools/serve_web.js` at server start; ignored by `.gitignore`; `ORKA-yib8.6` Fallow profile ignores its unresolved static import. | No cleanup beyond ensuring it remains ignored. |
| `.fallow/`, `.serena/`, `.netlify/`, `.worktrees/`, `rust/simulation_core/target/`, `node_modules/` | Keep ignored local caches | Ignored directories reported by `git status --ignored --short`; not tracked cleanup candidates except `node_modules/.package-lock.json`. | No repo change. Local cleanup only with explicit approval and never inside a code PR. |
| `package.json` `"main": "index.js"` | Separate package-metadata bead | `index.js` is absent. This is metadata hygiene, not generated-artifact deletion. | Create a scoped package metadata PR to remove or correct `main` after confirming package consumers and deploy tooling. |

## Required Follow-Up Beads

1. Package metadata cleanup: handle `node_modules/.package-lock.json` and package `main` mismatch together, without dependency upgrades.
2. Proof artifact retention policy: decide which `test-results/` and `output/web-game/` proofs stay tracked, move to archive, or get untracked.
3. Beads backup policy: decide `.beads/backup` ownership separately from generated-artifact cleanup.
4. Tiny generated-trash cleanup: remove `.DS_Store` files only after confirming no tracked proof or backup files are included.

## PR Documentation Output Audit

Checked PR metadata updated since 2026-06-03 plus markdown additions in local/origin history. Completion-style docs were classified one by one as active, useful evidence, or quarantine.

Quarantined in `governance/audit/quarantine/pr-doc-output-2026-07-03/`:

- legacy `agents/` prompt/log files that forced completion reports outside Beads
- generated `governance/bead-reviews/` packet snapshots
- stale Beads GitHub export manifests, mappings, dry-run reports, publish plans, and publication ledgers
- one-time `docs/superpowers/plans/` rollback/implementation plans
- generic `reports/` generated report/action files

Deferred: `origin/main` root token-drift and one-time jcodemunch diagnosis docs were audited but not moved because this branch cannot record their original-path deletion without merging `origin/main`, and that merge conflicts in `.beads/interactions.jsonl`.

Kept active: DOX/AGENTS files, durable retrieval routing, product truth, reusable export safety/template docs, eval command notes, game-design research, and audit evidence already under dated audit/report paths.

Quarantine index: `governance/audit/quarantine/pr-doc-output-2026-07-03/INDEX.md`.

## Stop Conditions

Stop before deleting tracked artifacts, `.beads` backups, validation proofs, package lock files, or deployment/build config. Stop if cleanup would mix package metadata, proof retention, Beads recovery, and local-cache hygiene in one PR.

## Validation For This Review

- `git ls-files -ci --exclude-standard`
- `git status --ignored --short`
- `git ls-files` for named tracked candidates
- `du -sh` for candidate size estimates
- `file` for `output/web-game` and `test-results` proof types
- package metadata check for missing `index.js`
- `git diff --check`
