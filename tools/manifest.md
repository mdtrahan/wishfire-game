# Tools manifest

# List deterministic tools (one-sentence description each)

- `tools/health_check.py` — simple environment and connectivity check
- `tools/graphify_refresh.sh` — non-blocking post-hook graph maintenance helper that refreshes code graph state and flags docs/images for manual semantic update
